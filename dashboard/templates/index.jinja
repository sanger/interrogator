<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="shortcut icon"
          href="{{ url_for('static', filename='favicon.ico') }}"
          type="image/x-icon" />
    <link rel="icon"
          href="{{ url_for('static', filename='favicon.ico') }}"
          type="image/x-icon" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
          crossorigin="anonymous">
    <title>iSuite Dashboard | PSD-Sanger</title>
    <style>
      .body-bg {
        background-color: #9921e8;
        background-image: linear-gradient(315deg, #9921e8 0%, #5f72be 74%);
      }
      .circle {
        display: inline-block;
        width: 0.7em;
        height: 0.7em;
        border-radius: 50%;
        border: 1px solid #333;
      }
      .status-success {
        color: var(--bs-success);
      }
      .status-failed {
        color: var(--bs-danger);
      }
      .status-in-progress {
        color: var(--bs-warning);
      }
      .status-canceled {
        color: var(--bs-gray);
      }
      .status-running {
        color: var(--bs-info);
      }
    </style>
  </head>
  <body class="body-bg container-xl p-4">
    <main class="bg-white p-8 rounded-4 shadow-lg">
      <div class="container p-4">
        <h1>Integration Suite Runs</h1>
        <div class="row mx-2">
          <span class="col-2">Created At</span>
          <span class="col-1">Pipeline</span>
          <span class="col-2">Int-Suite branch</span>
          <span class="col-3">Sequencescape Branch</span>
          <span class="col-3">Limber Branch</span>
          <span class="col-1">Result</span>
        </div>
        {% for pipeline in pipelines %}
          <div class="card mt-3">
            <div class="card-header">
              <div class="row">
                <span class="col-2">{{ pipeline.createdAt | replace('T', ' ') | truncate(16, True, '') }}</span>
                <span class="col-1">
                  <a href="https://gitlab.internal.sanger.ac.uk/psd/integration-suite/-/pipelines/{{ pipeline.id }}"
                     class="underline">{{ pipeline.id }}</a>
                </span>
                <span class="col-2">
                  <span class="circle me-1"
                        style="background-color: #{{ pipeline.commit_abbr }}"></span>
                  <code title="{{ pipeline.ref }}@{{ pipeline.commit_abbr }}">{{ pipeline.ref | truncate(20, True) }}@{{ pipeline.commit_abbr }}</code>
                </span>
                <span class="col-3">
                  <a href="{{ pipeline.versions.sequencescape_url }}">
                    <code title="{{ pipeline.versions.sequencescape_version }}">{{ pipeline.versions.sequencescape_version | truncate(27, True) }}</code>
                  </a>
                </span>
                <span class="col-3">
                  <a href="{{ pipeline.versions.limber_url }}">
                    <code title="{{ pipeline.versions.limber_version }}">{{ pipeline.versions.limber_version | truncate(27, True) }}</code>
                  </a>
                </span>
                <span class="col-1">
                  <span class="status-{{ pipeline.status | lower }}"><strong>{{ pipeline.status | title }}
                    {% if pipeline.status.lower() =='failed' %}- {{ pipeline.failed_tests | length }}{% endif %}
                  </strong>
                </span>
              </span>
            </div>
          </div>
          {% if pipeline.failed_tests %}
            <ul class='list-group list-group-flush'>
              {% for failed_test in  pipeline.failed_tests %}
                <li class='list-group-item py-1 small'>
                  <span class="font-monospace">{{ failed_test.ref }}</span><span class="ms-2 text-secondary">{{ failed_test.comment }}</span>
                </li>
              {% endfor %}
            </ul>
          {% endif %}
        </div>
      {% else %}
        <div class="card">
          <div class="card-body text-center text-secondary">
            No Integration Suite runs found, please check access to GitLab instance
          </div>
        </div>
      {% endfor %}
    </div>
  </main>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
          integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
          crossorigin="anonymous"></script>
</body>
</html>
